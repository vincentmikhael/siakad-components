import {TableDefaultDetail} from "./index";

export default function TableRuangan({data, columns, pinnedColumns}) {
    return <TableDefaultDetail data={data} columns={columns} pinnedColumns={pinnedColumns}/>
}